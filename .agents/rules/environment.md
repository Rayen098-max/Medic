# Environment Constraints

- **Shell**: The default shell in this environment is Windows PowerShell.
- **Command Chaining**: Do NOT use `&&` to chain commands in terminal instructions. Use `;` instead.
- **File Creation**: Do NOT use Bash heredocs (`cat << EOF > file`) in the terminal. Always use the provided tools to create or edit scripts and files.
